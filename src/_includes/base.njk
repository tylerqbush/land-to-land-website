<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or "Land to Land Holdings | Owner-Financed Land" }}</title>
  <meta name="description" content="{{ description or "Buy vacant land with no bank and no credit check. Simple monthly payments direct from the property owner at Land to Land Holdings." }}">
  <link rel="preload" href="/assets/fonts/dm-sans-normal.woff2" as="font" type="font/woff2" crossorigin>
  <link rel="stylesheet" href="/assets/css/styles.css">
  <!-- GHL TRACKING SCRIPT -->
</head>
<body{% if sale.active %} class="has-sale-banner"{% endif %}>
  {% if sale.active %}{% include "partials/sale-banner.njk" %}{% endif %}
  {% include "partials/nav.njk" %}
  {{ content | safe }}
  {% include "partials/footer.njk" %}
  <!-- GHL CHAT WIDGET -->
  <script src="/assets/js/animations.js" defer></script>
  {% if sale.active %}<script src="/assets/js/sale-banner.js" defer></script>{% endif %}
</body>
</html>
